#include <stdio.h>
#include <math.h>	//para q funcione el rand
#include <stdlib.h>

extern float* normalizar(int* v, unsigned short n);


int main(){
	srand(time(NULL));
	int i=0;
	//for (i=0;i<256;i+=1)
		return 0;
}
